import { ReactNode } from 'react';

interface SlugifyOptions {
    delimiter?: string;
    prefix?: string;
}
/**
 * Slugify a React node
 */
declare function slugify(node: ReactNode, options?: SlugifyOptions): string;

export { slugify as default };
